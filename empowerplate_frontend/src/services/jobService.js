// jobService
import axios from "axios";
import config from "../config/config.js";

export const createJob = async (job_name, job_contact) => {} // job_name would be from an enum

export const updateJobStatus = async (_id, status) => {}